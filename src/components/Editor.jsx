import React from "react";

const Editor = () => {
  return (
    <div className="w-full h-full">
      <textarea name="editor" id="editor" className="w-full h-full"></textarea>
    </div>
  );
};

export default Editor;
